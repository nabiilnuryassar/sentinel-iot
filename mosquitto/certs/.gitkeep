# TLS certificates for the Mosquitto :8883 listener live here.
# Generate them with: ./mosquitto/gen-certs.sh <broker-hostname>
# The generated *.key / *.crt files are gitignored (secrets) — only this
# placeholder is committed so the docker-compose bind mount has a target.
