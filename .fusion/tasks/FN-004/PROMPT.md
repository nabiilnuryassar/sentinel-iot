# FN-004

Phase 3b of Sentinel-IoT: UI Library Setup.

Tasks P3.13–P3.19:
- Initialize shadcn/ui.
- Install primitives (button, card, table, dialog, sheet, tabs, badge, input, select, dropdown-menu, sonner, skeleton, separator, scroll-area, textarea, label).
- Install dependencies (recharts, react-markdown, remark-gfm, date-fns, lucide-react, @tanstack/react-table).
- Mount sonner `<Toaster />`.
- Create domain component shells (severity-badge, status-pill, stat-card, telemetry-chart, data-table, markdown-view).
- Add format helpers.
